/* Topology canvas geometry. W/H stay fixed because saved node positions in
 * localStorage are absolute coordinates inside this viewBox. */

export const W = 1000;
export const H = 620;
export const CX = W / 2;
export const CY = H / 2 - 10;
export const NODE_R = 34;
